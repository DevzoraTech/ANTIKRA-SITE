# Optional. When omitted, Console requests use the same-origin /api/console path.
VITE_ANTIKRA_CONSOLE_API_URL=https://api.antikra.com/console

# Optional. When omitted, contact requests use the same-origin /api/contact path.
VITE_ANTIKRA_CONTACT_API_URL=https://api.antikra.com/contact
